{
  "name": "iot-display-hub-server",
  "version": "0.1.0",
  "description": "Backend Node.js per IoT Display Hub",
  "main": "src/app.js",
  "scripts": {
    "test": "jest",
    "start": "node src/app.js",
    "dev": "node --watch src/app.js",
    "lint": "oxlint src tests",
    "test:coverage": "jest --coverage"
  },
  "type": "commonjs",
  "private": true,
  "dependencies": {
    "express": "^4.22.3"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "oxlint": "^0.9.10",
    "supertest": "^7.2.2"
  }
}
